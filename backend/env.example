# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and replace all values with organization keys
# DO NOT commit .env file to version control

# ============================================
# DATABASE CONFIGURATION
# ============================================
# For MySQL/PostgreSQL (Production):
DATABASE_URL=mysql+pymysql://username:password@host:3306/database_name
# For SQLite (Development only):
# DATABASE_URL=sqlite:///./novabot.db

# ============================================
# SECURITY KEYS (CRITICAL - MUST BE REPLACED)
# ============================================
# JWT Secret Key - Used for signing authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-generate-a-new-one

# Encryption Key - Used for encrypting IT passwords and email passwords
# Generate with: python generate_key.py
# ⚠️ WARNING: If you change this key, all encrypted passwords must be re-encrypted
IT_ENCRYPTION_KEY=your-fernet-encryption-key-here

# ============================================
# AI API KEYS
# ============================================
# Google Gemini API Key (Required for chatbot)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI API Key (Optional - only if using OpenAI)
OPENAI_API_KEY=your-openai-api-key-here

# ============================================
# EMAIL SERVER CONFIGURATION (Gmail SMTP)
# ============================================
# Gmail SMTP settings for email sending
# These are used by backend/app/utils/email.py
# For Gmail, username is the email address and password is the Gmail app password
GMAIL_SMTP_SERVER=smtp.gmail.com
GMAIL_SMTP_PORT=587
# Note: Store your Gmail app password in the email_accounts table (encrypted)
# The app password will be used when authenticating with Gmail SMTP
# To generate app password: Google Account > Security > 2-Step Verification > App passwords

# ============================================
# CORS CONFIGURATION
# ============================================
# Allowed origins for CORS (comma-separated)
# When allow_credentials=True, you must specify exact origins (cannot use "*")
# Production example:
# ALLOWED_ORIGINS=https://hronboarding.sumerudigital.com,https://www.hronboarding.sumerudigital.com
# Development example:
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173
# If not set, defaults will be used (see backend/app/main.py)
ALLOWED_ORIGINS=

